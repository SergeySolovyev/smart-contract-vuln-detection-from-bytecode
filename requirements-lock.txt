# Версии, на которых получены опубликованные числа.
#
# Это НЕ минимальные требования, а зафиксированный слепок среды автора:
# `pip install -r requirements-lock.txt` воспроизводит её, а не «что-то
# совместимое». Разница существенна для сравнения с опубликованными
# значениями — на другой сборке scikit-learn пороговые метрики (точность,
# полнота, MCC) расходятся примерно на 1.6e-3, потому что несколько
# пограничных контрактов переходят через порог. Сам F1 при этом
# воспроизводится с точностью около 1e-5.
#
# Именно поэтому допуски в ноутбуке воспроизводимости различаются по типам
# величин: 5e-4 на заглавные метрики, 2e-3 на пороговые компоненты, 1e-2 на
# редкие классы. Подробности — в 05_notebooks/repro_v2.ipynb.

numpy==1.26.4
pandas==2.3.3
pyarrow==21.0.0
scikit-learn==1.8.0
xgboost==3.1.2
catboost==1.2.8
scipy==1.16.3
matplotlib==3.10.8
seaborn==0.13.2

# Только для шага 3 (обучение глубоких конфигураций на видеокарте).
# Сборка torch должна соответствовать вашей CUDA; на Kaggle T4 (sm_75)
# штатная сборка образа падала с cudaErrorNoKernelImageForDevice, лечилось
# переустановкой на cu124.
torch==2.10.0

# Только для сторожа: он читает СОБРАННЫЙ PDF, а не исходник.
pymupdf==1.27.2

# Python 3.12.8 на Windows 11. LaTeX: pdflatex + bibtex (MiKTeX или TeX Live).
